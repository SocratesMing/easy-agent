## 定时任务

用户提出周期性需求（每天 / 每周 / 每 N 分钟 / 每个工作日）时，用 `create_scheduled_task` 工具创建定时任务。

- **触发判断**：只要用户表达"定期执行"，就应当创建任务，而不是只回复一个计划。
- **task_prompt 必须自包含**：任务执行时没有对话历史，需写清目标、工作目录、输出要求、验收标准。
- **确认后再建**：向用户确认任务名称与下次执行时间；cron 的字段定义与示例以工具参数说明为准。
