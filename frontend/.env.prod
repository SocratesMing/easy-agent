# 生产环境配置 (npm run build)
# 生产环境：前端由 `serve -s dist/` 独立静态托管，后端在独立地址。
# 必须写后端绝对地址（相对路径 "/" 会打到 serve 自身端口导致 404）；
# 跨域由后端 CORS 放行（默认 allow_origins="*"，生产建议设 EASY_CORS_ALLOW_ORIGINS）。
# 若后端实际地址不同，请修改此处。
VITE_API_BASE_URL=http://192.168.1.200:8000
VITE_APP_TITLE=Easy Agent
