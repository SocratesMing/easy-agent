# 生产环境配置 (npm run build)
# 生产环境：前端由 `serve -s dist/` 独立静态托管，后端在独立地址。
# 默认同源；独立静态托管时由 runtime-config.js 或环境变量注入后端地址。
VUE_APP_API_BASE_URL=
VUE_APP_TITLE=Easy Agent
VUE_APP_WELCOME_TITLE=Easy Agent，让工作更简单
