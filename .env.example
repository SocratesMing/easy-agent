# ============================================================
# Easy Agent 环境变量模板
#
# 用法: cp .env.example .env  然后填入真实值（.env 已被 .gitignore 忽略）
#
# 说明:
#   - 应用启动早期自动加载项目根 .env，与启动方式无关（python main.py / IDE / easy-web）。
#   - 不覆盖进程里已有的同名环境变量；需要 .env 优先时设置 EASY_ENV_OVERRIDE=1。
#   - YAML 配置里的 ${VAR} / ${VAR:-默认值} 占位符在解析时用这里的变量填充，
#     因此 API Key、数据库密码等敏感值建议只写在 .env，不写进配置文件。
#   - 改完需重启后端。

# ===== 配置文件引用的变量（与 config.yaml 中的 ${...} 对应） =====
# 当前 active model（config.yaml 的 model 字段）对应的 Key 必须设置，
# 未取到值的占位符会按空值处理并在启动日志里 WARNING 列出变量名。
# DEEPSEEK_API_KEY=
# MINIMAX_API_KEY=
# ARK_API_KEY=
# GLM_API_KEY=
# MYSQL_PASSWORD=

# ============================================================

# ===== 运行时控制变量 =====
# JWT 密钥：不设置则每次启动随机生成，重启后所有登录态失效
# EASY_JWT_SECRET=

# 指定配置文件路径（优先级高于 AGENT_ENV）
# EASY_CONFIG=/path/to/config.yaml

# 运行环境: dev | test | prod
# AGENT_ENV=dev

# ===== MySQL 兜底连接（主应用 database.mysql 优先；独立运行 Market MCP 时使用） =====
# MYSQL_HOST=127.0.0.1
# MYSQL_PORT=3306
# MYSQL_USER=root
# MYSQL_PASSWORD=
# MYSQL_DATABASE=agent
