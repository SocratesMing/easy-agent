# 生产环境（npm run build，默认 mode=prod）
# 生产不使用前端代理：dist/ 由后端同源托管（或由 Nginx 反代 /agent），
# 前端相对路径请求直接命中后端，因此无需配置后端地址。
# 参考地址见 vue.config.js 的 PROXY_TARGETS.prod（默认 http://192.168.1.200:8080）
VUE_APP_AGENT_ENV=prod
